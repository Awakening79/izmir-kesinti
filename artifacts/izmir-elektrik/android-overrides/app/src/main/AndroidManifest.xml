<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml override for İzmir Kesinti Takibi.
  After `npx cap add android`, MERGE the <activity> attributes below
  into android/app/src/main/AndroidManifest.xml.

  Key settings applied here:
    • screenOrientation="portrait"   → locks to Hochformat
    • android:theme="@style/AppTheme.Launcher" → fullscreen splash
    • FLAG_FULLSCREEN via theme      → hides status bar at runtime
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.izmir.kesintitakibi">

    <!-- Internet access (required for API calls) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: network state detection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="İzmir Kesinti Takibi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"

            android:screenOrientation="portrait"

            android:theme="@style/AppTheme.Launcher"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
