# DEAKTIVIERT — wird durch android-build.yml ersetzt
on: workflow_dispatch
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dieser Workflow ist deaktiviert. Bitte android-build.yml verwenden."
